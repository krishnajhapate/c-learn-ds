
#include <stdio.h>


// Defining a node for linked list
struct Node
{
    int data;
    struct Node *next;
};

// Creating Node for linked list
struct Node * createNode()
{
    struct Node *temp;
    temp
};

